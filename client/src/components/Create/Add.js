export default function Add(props) {
  return (
    <>
      <button
        onClick={props.onAddTask}
      >
        Add Task
      </button>
      <button>
        Generate Route (WIP)
      </button>
    </>
  )
}